import test from 'node:test';import assert from 'node:assert/strict';import {Governor} from '../server/governor.js';class S{async init(d){return structuredClone(d)}async write(){}}test('governor blocks unverified revenue and spending',async()=>{const g=new Governor(new S());await g.init();assert.equal(g.status().policy.allowSpending,false);const x=await g.pending({provider:'TEST',providerRef:'1',amountEUR:10});await assert.rejects(()=>g.verify({settlementId:x.id,provider:'TEST',providerRef:'1',amountEUR:10,verification:{verified:false}}),/TRUSTED_VERIFICATION_REQUIRED/);assert.equal(g.status().metrics.verifiedRevenueEUR,0);});
